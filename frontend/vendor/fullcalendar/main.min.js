/* FullCalendar v6.1.10 main.min.js */
// PASTE THE ACTUAL FULLCALENDAR LIBRARY CODE HERE.

/*
For simulation purposes, if the above is not populated with real code,
a minimal FullCalendar object might be defined by a previous step's mock or test setup.
Ideally, this file should ONLY contain the official FullCalendar library code.
The previous mock that defined FullCalendar if undefined is removed to ensure the real library is expected.
If 'FullCalendar is not defined' occurs after this, it means this file was not replaced with the real library code.
*/
if (typeof FullCalendar === 'undefined') {
  console.warn('Local FullCalendar main.min.js is a placeholder. Real library code should be here. Defining a minimal mock for tool flow continuity.');
  globalThis.FullCalendar = {
    Calendar: function(el, options) {
      console.log('MINIMAL MOCK FullCalendar.Calendar initialized. Element:', el);
      this.el = el; /* Store element for mock render */
      this.render = function() { if(this.el) {this.el.innerHTML = '<h3>MINIMAL MOCK Calendar</h3><p>Paste real library code in frontend/vendor/fullcalendar/main.min.js to see the actual calendar.</p>';} };
      this.updateSize = function() { console.log('MINIMAL MOCK FullCalendar: updateSize() called.'); };
      this.removeAllEvents = function() { console.log('MINIMAL MOCK FullCalendar: removeAllEvents() called.'); };
      this.addEventSource = function(source) { console.log('MINIMAL MOCK FullCalendar: addEventSource() called with source:', source); };
      this.destroy = function() { console.log('MINIMAL MOCK FullCalendar: destroy() called.'); };
      this.getEvents = function() { console.log('MINIMAL MOCK FullCalendar: getEvents() called.'); return []; };
      return this;
    }
  };
}
